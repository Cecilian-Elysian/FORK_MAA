# 账号轮换功能设计与实现

## 概述

账号轮换功能允许用户在 Arknights 中配置多个账号，MAA 在每次完整任务队列执行完毕后自动切换到列表中的下一个未完成账号，并重新启动任务队列，直到所有账号全部完成一轮。

---

## 一、功能架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        StartUpTaskUserControl.xaml               │
│  (WPF UI: 账号切换开关 / 轮换开关 / 编辑模式 / 账号列表增删)      │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 绑定
┌──────────────────────────▼───────────────────────────────────────┐
│                 StartUpSettingsUserControlModel.cs                │
│  (ViewModel: AccountSwitchEnabled / AccountCycleEnabled /         │
│   AccountCycleItems / SyncAccountNamesToItems / AddAccountAfter   │
│   RemoveAccount / GetCurrentCycleAccount / MarkAccountCompleted)  │
└──────────┬────────────────────────────────────┬──────────────────┘
           │ 序列化                              │ 配置读写
┌──────────▼──────────┐             ┌───────────▼──────────────────┐
│  TaskQueueViewModel  │             │  StartUpTask.cs (Config)     │
│  (LinkStart 轮换调度 │             │  AccountCycleEnabled /       │
│   TryStartNextCycle) │             │  AccountNames / AccountName) │
└──────────┬───────────┘             └──────────────────────────────┘
           │ 异步任务
┌──────────▼───────────────────────────────────────────────────────┐
│  C++ Core — AccountSwitchTask / StartUpTask                      │
│  (实际游戏内操作: 退出登录 → 账号管理 → OCR识别 → 选中 → 登录)   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 二、数据模型

### 2.1 AccountCycleItem

`src/MaaWpfGui/Models/AccountCycleItem.cs`

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `DisplayName` | `string` | `""` | 显示标签，如"账号1""账号2"（仅展示，不持久化） |
| `AccountName` | `string` | `""` | 实际登录凭据（用户输入，持久化到配置） |
| `IsSelected` | `bool` | `true` | 是否参与轮换 |
| `IsCompleted` | `bool` | `false` | 本轮是否已完成（蓝色背景标识） |
| `Index` | `int` | `0` | 列表中的排序索引 |

继承 `Stylet.PropertyChangedBase`，所有属性均支持 WPF 数据绑定。

### 2.2 StartUpTask（配置模型）

`src/MaaWpfGui/Configuration/Single/MaaTask/StartUpTask.cs`

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `AccountName` | `string` | `""` | 单账号切换的目标账号名 |
| `AccountSwitchEnabled` | `bool?` | `true` | 是否启用账号切换功能（null 表示旧配置未迁移） |
| `AccountCycleEnabled` | `bool` | `false` | 是否启用账号轮换 |
| `AccountNames` | `List<string>` | `[]` | 轮换账号列表 |

`OnDeserialized()`：旧配置迁移逻辑 — 若 `AccountSwitchEnabled` 为 null（旧 JSON 没有此字段），则根据 `AccountName` 是否非空设为对应值。

---

## 三、编辑模式

用户通过"编辑模式"复选框 + ComboBox（无修改 / 添加账号 / 删除账号）控制账号列表的编辑状态。

### 3.1 状态控制

| 绑定路径 | 类型 | 说明 |
|---------|------|------|
| `ShowEditSection` | `bool` | "编辑模式"复选框的绑定；关闭时自动重置 `AccountCycleMode = 0` |
| `AccountCycleMode` | `int` | 0=无修改, 1=添加账号, 2=删除账号 |
| `ShowAddMode` | `bool` | `AccountCycleMode == 1` 的计算结果 |
| `ShowDeleteMode` | `bool` | `AccountCycleMode == 2` 的计算结果 |

### 3.2 添加账号（AddAccountAfter）

- 在当前账号项（`item.Index`）之后插入一个新账号
- 新账号 `AccountName = ""`，`IsSelected = true`，`IsCompleted = false`
- 配置列表 `AccountNames` 同步插入空字符串
- 插入后重建所有项的 `Index`（保持连续 0..n-1）
- 添加按钮仅在 `ShowAddMode == true` 时可见
- 样式：`hc:IconElement.Geometry="{StaticResource AddGeometry}"`，10×10 图标，透明背景，与删除按钮风格一致

### 3.3 删除账号（RemoveAccount）

- 从配置列表 `AccountNames` 中移除对应项
- 从 `_accountCycleItems` 集合中移除
- 从 `_completedAccounts` 记录中移除
- 取消 `PropertyChanged` 订阅（避免内存泄漏）
- 移除后重建所有项的 `Index`
- 删除完毕后 **不自动修改 `AccountCycleEnabled`**（由用户手动管理开关）
- 删除按钮仅在 `ShowDeleteMode == true` 时可见
- 样式：`hc:IconElement.Geometry="{StaticResource CloseGeometry}"`，10×10 图标，透明背景，`VisualElement.HighlightBackground` 透明 + `HighlightForeground` 主题色

### 3.4 实时持久化

`OnAccountCycleItemPropertyChanged()`：每个 `AccountCycleItem` 订阅 `PropertyChanged`，当 `AccountName` 发生变更时，通过 `SetTaskConfig` 实时写入 `AccountNames[item.Index]`，保障用户修改即保存。

---

## 四、账号轮换生命周期

### 4.1 启动流程（LinkStart）

```
用户点击"开始" → LinkStart()
  ├─ 检查 AccountCycleEnabled && 有已选中的AccountCycleItem
  │   ├─ true  → _isCycling = true
  │   │           startUpTask.AccountName = GetCurrentCycleAccount()（取第一个未完成账号）
  │   └─ false → ResetCycle()（重置轮换状态）
  └─ LinkStartWithTasks（执行任务队列）
```

### 4.2 串行化（SerializeTask）

`StartUpSettingsUserControlModel` 内部接口 `ISerialize`：

```csharp
var accountName = !UseAttachWindow &&
    clientType 属于 {Official, Bilibili, Txwy} &&
    AccountSwitchEnabled == true
        ? startUp.AccountName  // 使用当前 AccountName（轮换时由 LinkStart 或 TryStartNextCycleAccount 设置）
        : string.Empty;
```

轮换模式下不依赖文本框输入，`AccountName` 由 `GetCurrentCycleAccount()` 动态设置。

### 4.3 轮换推进（TryStartNextCycleAccount）

```
任务队列完成 → SetStopped() → TryStartNextCycleAccount()
  ├─ _isCycling == false → return
  ├─ AccountCycleEnabled != true → _isCycling = false; return
  ├─ 标记当前 AccountName 为已完成
  ├─ GetCurrentCycleAccount()
  │   ├─ 返回 null → 日志"所有账号已完成！"; _isCycling = false; return
  │   └─ 返回下一个账号 → startUpTask.AccountName = nextAccount
  │                       1.5s 延迟后自动重启 LinkStartWithTasks()
  └─ 重复直到所有账号完成
```

### 4.4 账号选取算法（GetCurrentCycleAccount）

```csharp
1. 筛选：IsSelected && !IsCompleted && AccountName 非空
2. 优先取 Index >= _currentCycleIndex 的候选
3. 若未找到，回退取第一个未完成的
4. 更新 _currentCycleIndex = candidate.Index + 1（推进到下一号）
5. 返回 candidate.AccountName
```

### 4.5 状态重置

| 方法 | 操作 | 触发时机 |
|------|------|---------|
| `ResetCycle()` | `_currentCycleIndex = 0` + `ClearCompletedAccounts()` | 用户重新开始（非轮换模式） |
| `ResetCycleIndex()` | `_currentCycleIndex = 0` | 仅重置索引，不清除完成状态 |
| `ClearCompletedAccounts()` | 清空 `_completedAccounts` + 所有 `IsCompleted = false` | 同上 |

---

## 五、UI 布局与交互

### 5.1 控件层级

```
├─ "账号切换" CheckBox (AccountSwitchEnabled)
│  └─ 文本框 + "立即切换" 按钮 Grid
│     [Visible: AccountSwitchEnabled && !AccountCycleEnabled]
│     ├─ TextBox (AccountName, 单账号输入)
│     └─ Button "立即切换" (AccountSwitchManualRun)
│
├─ "账号轮换" CheckBox (AccountCycleEnabled)
│  └─ ItemsControl (AccountCycleItems)
│     └─ 每项: [CheckBox (IsSelected)] [TextBox (AccountName)] [AddButton/DeleteButton]
│  ├─ "编辑模式" CheckBox (ShowEditSection)
│  └─ ComboBox: 无修改 / 添加账号 / 删除账号 (AccountCycleMode)
```

### 5.2 账号项模板

每行三列 Grid：
| 列 | 宽度 | 控件 | 绑定 |
|---|------|------|------|
| 0 | Auto | CheckBox | `IsSelected`，ToolTip = "勾选后该账号将参与轮换" |
| 1 | `*` | TextBox | `AccountName`，`UpdateSourceTrigger=PropertyChanged`，可编辑凭据 |
| 2 | Auto | Button (Add) | 显示条件 `ShowAddMode`，`AddGeometry` 图标，Command `AddAccountAfter`，ToolTip = "在该账号后添加新账号" |
| 2 | Auto | Button (Delete) | 显示条件 `ShowDeleteMode`，`CloseGeometry` 图标，Command `RemoveAccount` |

已完成账号（`IsCompleted == true`）的背景色变为 `#206C9CF3`（半透明蓝色，DataTrigger 驱动）。

### 5.3 编辑模式 ComboBox

- 三态选择：`无修改` (0) / `添加账号` (1) / `删除账号` (2)
- 关闭"编辑模式"CheckBox 时自动回到"无修改"
- 添加和删除按钮互斥显示

---

## 六、本地化字符串

`src/MaaWpfGui/Res/Localizations/zh-cn.xaml`（同步到 en-us, ja-jp, ko-kr, zh-tw）：

| Key | zh-CN |
|-----|-------|
| `AccountSwitch` | 账号切换 |
| `AccountSwitchManualRun` | 立即切换 |
| `AccountSwitchTip` | (详细提示文本) |
| `AccountCycle` | 账号轮换 |
| `AccountCycleEditMode` | 编辑模式 |
| `AccountCycleNewAccountDefaultName` | 账号 |
| `AccountCycleItemCheckTip` | 勾选后该账号将参与轮换 |
| `AccountCycleAddAccountTip` | 在该账号后添加新账号 |
| `AccountCycleAllDone` | 所有账号已完成！ |
| `AccountCycleSwitchingTo` | 正在切换至 |

---

## 七、手动切换（AccountSwitchManualRun）

| 模式 | 行为 |
|------|------|
| 轮换关闭 | 取文本框 `AccountName`，构建单任务 `StartUpTask`，执行 `LinkStartWithTasks` |
| 轮换开启 | 调用 `GetCurrentCycleAccount()` 取下一未完成账号，构建单任务 `StartUpTask`，执行 `LinkStartWithTasks`；若无可用账号 → 日志"所有账号已完成！" |

---

## 八、C++ 后端的账号切换流程

`resource/tasks/tasks.json` 中定义了约 30 个任务节点：

```
SwitchAccount@StartUpBegin
  → Settings（点击设置）
  → UserCenter（用户中心）
  → Logout（退出登录）
    → LogoutOCR（OCR识别"退出登录"/"账号"）
    → LogoutConfirm（确认退出）
  → AccountManager（账号管理）
    → 官服: AccountManagerOfficial（ROI匹配）
    → B服: AccountManagerBili（OCR匹配"登录记录"）
  → AccountCurrentOCR（OCR识别当前账号名，判断是否匹配目标）
    → 匹配 → AccountManagerLoginButton（点击登录）
    → 不匹配 → AccountManagerListAccount（展开列表）
      → swipe_and_select（滑动列表 + OCR找目标账号）
      → 选中 → AccountManagerLoginButton（点击登录）
  → 回到主界面
```

支持三种客户端：官方（Official）、Bilibili（B服）、天翼（txwy）。客户端差异通过 `AccountManagerTxwy`、`AccountManagerListAccountBili` 等任务节点处理。

---

## 九、默认配置

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `AccountSwitchEnabled` | `true` | 账号切换默认开启 |
| `AccountCycleEnabled` | `false` | 账号轮换默认关闭,这很重要 | 
| `AccountNames` | `["189****0830", ""]` | 示例配置 |

---

## 十、文件清单

| 文件 | 职责 |
|------|------|
| `src/MaaWpfGui/Models/AccountCycleItem.cs` | 轮换账号数据模型（DisplayName / AccountName / IsSelected / IsCompleted / Index） |
| `src/MaaWpfGui/Configuration/Single/MaaTask/StartUpTask.cs` | 配置模型（AccountCycleEnabled / AccountNames / 旧配置迁移） |
| `src/MaaWpfGui/ViewModels/UserControl/TaskQueue/StartUpSettingsUserControlModel.cs` | ViewModel（全量轮换业务逻辑：同步/增删/选取/标记/重置） |
| `src/MaaWpfGui/ViewModels/UI/TaskQueueViewModel.cs` | 队列编排（LinkStart 轮换判定 + TryStartNextCycleAccount 自动推进） |
| `src/MaaWpfGui/Views/UserControl/TaskQueue/StartUpTaskUserControl.xaml` | WPF 视图（编辑模式 ComboBox / 账号列表 ItemsControl / 图标按钮） |
| `src/MaaWpfGui/Res/Localizations/{zh-cn,en-us,ja-jp,ko-kr,zh-tw}.xaml` | 多语言本地化（10 个 key） |
| `src/MaaCore/Task/Miscellaneous/AccountSwitchTask.{h,cpp}` | C++ 执行层：游戏内账号切换操作 |
| `src/MaaCore/Task/Interface/StartUpTask.{h,cpp}` | C++ 编排层：启动 → 账号切换 → 主界面 |
| `resource/tasks/tasks.json` (lines 728–864) | 任务节点定义（~30 个节点，覆盖官服/B服/txwy/iOS） |
| `resource/template/WakeUp/AccountManager/*.png` | 模板图片（账号管理 UI 元素） |
| `resource/global/txwy/resource/tasks/tasks.json` | 天翼客户端 ROI 覆盖 |
| `resource/platform_diff/iOS/resource/tasks.json` | iOS 客户端 ROI 覆盖 |
| `install/config/gui.new.json` / `.bak` | 默认配置文件 |

---

## 十一、约束与注意事项

1. **单账号输入框在轮换开启时隐藏**：`Visibility="{c:Binding 'AccountSwitchEnabled and !AccountCycleEnabled'}"`，轮换模式下仅通过 ItemsControl 管理账号
2. **删除不自动关闭轮换**：保留 `AccountCycleEnabled` 的当前值，由用户手动控制
3. **新增账号自动选中**：`IsSelected = true`，参与下一轮轮换
4. **账号名不可为空**：`GetCurrentCycleAccount()` 会过滤 `!string.IsNullOrEmpty(x.AccountName)`
5. **非空名策略**：`SyncAccountNamesToItems()` 在 `AccountNames` 为空时自动填入当前 `AccountName + ""`
6. **LinkStart 首次运行**：`_isCycling=true` 时，`startUpTask.AccountName` 由 `GetCurrentCycleAccount()` 设置，不从文本框读值
7. **生成操作报告DOING.md再install文件夹中,包括UI怎么布置,逻辑怎么写出来等**