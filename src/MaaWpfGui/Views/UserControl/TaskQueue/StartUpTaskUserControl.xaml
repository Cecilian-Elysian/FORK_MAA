<UserControl
    x:Class="MaaWpfGui.Views.UserControl.TaskQueue.StartUpTaskUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:settings_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.Settings"
    xmlns:taskQueue_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.TaskQueue"
    xmlns:ui_vms="clr-namespace:MaaWpfGui.ViewModels.UI"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type taskQueue_vms:StartUpSettingsUserControlModel}}"
    d:DesignWidth="220"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <StackPanel>
        <StackPanel Visibility="{c:Binding !UseAttachWindow, Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}">
            <StackPanel Visibility="{c:Binding 'ClientType == &quot;Official&quot; or ClientType == &quot;Bilibili&quot; or ClientType == &quot;txwy&quot;', Source={x:Static settings_vms:GameSettingsUserControlModel.Instance}}">
                <StackPanel
                    Margin="0,5"
                    HorizontalAlignment="Left"
                    Orientation="Horizontal">
                    <CheckBox Content="{DynamicResource AccountSwitch}" IsChecked="{Binding AccountSwitchEnabled}" />
                    <controls:TooltipBlock TooltipText="{DynamicResource AccountSwitchTip}" />
                </StackPanel>
                <Grid Margin="0,5" Visibility="{c:Binding 'AccountSwitchEnabled and !AccountCycleEnabled'}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        hc:BorderElement.CornerRadius="4,0,0,4"
                        IsReadOnly="{c:Binding !Idle,
                                               Source={x:Static helper:Instances.SettingsViewModel}}"
                        Text="{Binding AccountName, UpdateSourceTrigger=PropertyChanged}" />
                    <Button
                        Grid.Column="1"
                        Padding="12,1,12,1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        hc:BorderElement.CornerRadius="0,4,4,0"
                        BorderThickness="0,1,1,1"
                        Command="{s:Action AccountSwitchManualRun}"
                        Content="{DynamicResource AccountSwitchManualRun}"
                        IsEnabled="{c:Binding 'AccountName != &quot;&quot;'}" />
                </Grid>

                <!--  Account Cycle Section  -->
                <StackPanel Margin="0,5" HorizontalAlignment="Left" Orientation="Horizontal">
                    <CheckBox Content="{DynamicResource AccountCycle}" IsChecked="{Binding AccountCycleEnabled}" />
                </StackPanel>
                <StackPanel Margin="0,5" Visibility="{c:Binding AccountCycleEnabled}">
                    <ItemsControl ItemsSource="{Binding AccountCycleItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,2" CornerRadius="4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                    <Setter Property="Background" Value="#206C9CF3" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            IsChecked="{Binding IsSelected}"
                                            ToolTip="{DynamicResource AccountCycleItemCheckTip}" />
                                        <TextBox
                                            Grid.Column="1"
                                            Margin="4,0"
                                            hc:BorderElement.CornerRadius="4"
                                            Text="{Binding AccountName, UpdateSourceTrigger=PropertyChanged}" />
                                        <Button
                                            Grid.Column="2"
                                            Width="20"
                                            Height="20"
                                            Padding="0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Command="{s:Action AddAccountAfter}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{DynamicResource AccountCycleAddAccountTip}"
                                            Visibility="{c:Binding ShowAddMode, Source={x:Static taskQueue_vms:StartUpSettingsUserControlModel.Instance}}">
                                            <TextBlock Text="+" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Button>
                                        <Button
                                            Grid.Column="2"
                                            Width="20"
                                            Height="20"
                                            Padding="0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Command="{s:Action RemoveAccount}"
                                            CommandParameter="{Binding}"
                                            Visibility="{c:Binding ShowDeleteMode, Source={x:Static taskQueue_vms:StartUpSettingsUserControlModel.Instance}}">
                                            <TextBlock Text="✖" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <StackPanel Margin="0,5" HorizontalAlignment="Left" Orientation="Horizontal">
                        <CheckBox Content="{DynamicResource AccountCycleEditMode}" IsChecked="{Binding ShowEditSection}" />
                    </StackPanel>
                    <ComboBox
                        Margin="0,5"
                        hc:InfoElement.Title="{DynamicResource AccountCycleEditMode}"
                        SelectedIndex="{Binding AccountCycleMode}"
                        IsEnabled="{Binding ShowEditSection}">
                        <ComboBoxItem>无修改</ComboBoxItem>
                        <ComboBoxItem>添加账号</ComboBoxItem>
                        <ComboBoxItem>删除账号</ComboBoxItem>
                    </ComboBox>

                    <!--  feat/defer-rogue: Late stage rogue & reclamation  -->
                    <StackPanel Margin="0,5" HorizontalAlignment="Left" Orientation="Horizontal">
                        <CheckBox
                            MaxWidth="{c:Binding '220 - ActualWidth',
                                                 ElementName=LateStageRogueAndReclamationTipBlock}"
                            IsChecked="{Binding LateStageRogueAndReclamation}">
                            <TextBlock
                                Block.TextAlignment="Left"
                                Text="{DynamicResource LateStageRogueAndReclamation}"
                                TextWrapping="Wrap" />
                        </CheckBox>
                        <controls:TooltipBlock
                            x:Name="LateStageRogueAndReclamationTipBlock"
                            TooltipText="{DynamicResource LateStageRogueAndReclamationTip}" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <controls:TextBlock
                Margin="0,5"
                HorizontalAlignment="Center"
                FontWeight="Bold"
                Text="{DynamicResource MultiTasksShareTip}"
                TextAlignment="Left"
                TextWrapping="Wrap" />
            <CheckBox
                Margin="0,10"
                Content="{DynamicResource StartGameLaunchClient}"
                IsChecked="{Binding StartGame, Source={x:Static settings_vms:GameSettingsUserControlModel.Instance}}" />
            <hc:ComboBox
                Margin="0,5"
                hc:InfoElement.Title="{DynamicResource ClientType}"
                DisplayMemberPath="Display"
                IsHitTestVisible="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                ItemsSource="{Binding ClientTypeList, Source={x:Static settings_vms:GameSettingsUserControlModel.Instance}}"
                SelectedValue="{Binding ClientType, Source={x:Static settings_vms:GameSettingsUserControlModel.Instance}}"
                SelectedValuePath="Value"
                Visibility="{c:Binding !TaskSettingVisibilities.Guide,
                                       Source={x:Static helper:Instances.SettingsViewModel}}" />
        </StackPanel>

        <StackPanel s:View.ActionTarget="{Binding DataContext, RelativeSource={RelativeSource Self}}" DataContext="{Binding Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}">
            <StackPanel Visibility="{c:Binding '!UseAttachWindow'}">
                <StackPanel Margin="0,10" Orientation="Horizontal">
                    <CheckBox Content="{DynamicResource AutoDetectConnection}" IsChecked="{Binding AutoDetectConnection}" />
                    <controls:TooltipBlock TooltipText="{DynamicResource AutoDetectConnectionTip}" />
                </StackPanel>
                <StackPanel
                    Margin="0,10"
                    Orientation="Horizontal"
                    Visibility="{c:Binding AutoDetectConnection}">
                    <CheckBox
                        Content="{DynamicResource AlwaysAutoDetectConnection}"
                        IsChecked="{Binding AlwaysAutoDetectConnection}"
                        Style="{StaticResource ErrorHighlightCheckBoxStyle}" />
                    <controls:TooltipBlock TooltipText="{DynamicResource AlwaysAutoDetectConnectionTip}" />
                </StackPanel>
            </StackPanel>

            <!--  连接配置  -->
            <hc:ComboBox
                Margin="0,5"
                hc:InfoElement.Title="{DynamicResource ConnectionPreset}"
                DisplayMemberPath="Display"
                ItemsSource="{Binding ConnectConfigList}"
                SelectedValue="{Binding ConnectConfig}"
                SelectedValuePath="Value" />

            <TextBlock
                Margin="10,0"
                HorizontalAlignment="Center"
                Foreground="Red"
                Text="{DynamicResource UseAttachWindowWarning}"
                TextWrapping="Wrap"
                Visibility="{c:Binding UseAttachWindow}" />

            <!--  ADB 连接配置区域  -->
            <StackPanel Visibility="{c:Binding '!UseAttachWindow'}">
                <Grid Margin="0,5" Visibility="{c:Binding !AutoDetectConnection}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <hc:TextBox
                        Grid.Column="0"
                        hc:BorderElement.CornerRadius="4,0,0,4"
                        hc:InfoElement.Title="{DynamicResource AdbPath}"
                        Text="{Binding AdbPath}" />
                    <Button
                        Grid.Column="1"
                        Width="60"
                        VerticalAlignment="Bottom"
                        hc:BorderElement.CornerRadius="0,4,4,0"
                        BorderThickness="0,1,1,1"
                        Command="{s:Action SelectFile}"
                        Content="{DynamicResource Select}" />
                </Grid>

                <Grid Margin="0,5" Visibility="{c:Binding !AutoDetectConnection}">
                    <StackPanel
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Orientation="Horizontal">
                        <TextBlock
                            Margin="8,0,0,0"
                            Text="{DynamicResource ConnectionAddress}"
                            Visibility="Hidden" />
                        <controls:TooltipBlock Margin="0,0,5,0" TooltipText="{DynamicResource ConnectionAddressTip}" />
                    </StackPanel>
                    <hc:ComboBox
                        HorizontalContentAlignment="Stretch"
                        hc:InfoElement.Title="{DynamicResource ConnectionAddress}"
                        IsEditable="True"
                        ItemsSource="{Binding ConnectAddressHistory}"
                        Text="{Binding ConnectAddress, UpdateSourceTrigger=LostFocus}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Text="{Binding}" />
                                    <Button
                                        Grid.Column="1"
                                        Width="30"
                                        Height="30"
                                        HorizontalAlignment="Right"
                                        Command="{s:Action RemoveAddressClick}"
                                        CommandParameter="{Binding}"
                                        Content="×"
                                        FontWeight="Bold" />
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </hc:ComboBox>
                </Grid>

                <hc:ComboBox
                    Margin="0,5"
                    hc:InfoElement.Title="{DynamicResource TouchMode}"
                    DisplayMemberPath="Display"
                    IsHitTestVisible="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding TouchModeList}"
                    SelectedValue="{Binding TouchMode}"
                    SelectedValuePath="Value" />
            </StackPanel>

            <!--  PC 窗口绑定配置区域  -->
            <StackPanel Visibility="{c:Binding UseAttachWindow}">
                <hc:ComboBox
                    Margin="0,5"
                    hc:InfoElement.Title="{DynamicResource AttachWindowScreencapMethod}"
                    DisplayMemberPath="Display"
                    ItemsSource="{Binding AttachWindowScreencapMethodList}"
                    SelectedValue="{Binding AttachWindowScreencapMethod}"
                    SelectedValuePath="Value" />

                <hc:ComboBox
                    Margin="0,5"
                    hc:InfoElement.Title="{DynamicResource AttachWindowMouseMethod}"
                    DisplayMemberPath="Display"
                    ItemsSource="{Binding AttachWindowMouseMethodList}"
                    SelectedValue="{Binding AttachWindowMouseMethod}"
                    SelectedValuePath="Value" />

                <hc:ComboBox
                    Margin="0,5"
                    hc:InfoElement.Title="{DynamicResource AttachWindowKeyboardMethod}"
                    DisplayMemberPath="Display"
                    ItemsSource="{Binding AttachWindowKeyboardMethodList}"
                    SelectedValue="{Binding AttachWindowKeyboardMethod}"
                    SelectedValuePath="Value" />
            </StackPanel>

            <!--  截图增强（MuMu12）  -->
            <StackPanel Visibility="{c:Binding 'ConnectConfig==&quot;MuMuEmulator12&quot; and !UseAttachWindow'}">
                <StackPanel
                    Margin="0,5"
                    HorizontalAlignment="Left"
                    Orientation="Horizontal">
                    <CheckBox Content="{DynamicResource MuMu12ExtrasEnabled}" IsChecked="{Binding MuMuEmulatorExtras.Enable}" />
                    <controls:TooltipBlock TooltipText="{DynamicResource MuMu12ExtrasEnabledTip}" />
                </StackPanel>
                <StackPanel
                    HorizontalAlignment="Left"
                    Orientation="Vertical"
                    Visibility="{c:Binding MuMuEmulatorExtras.Enable}">
                    <hc:TextBox
                        Margin="0,5"
                        hc:InfoElement.Placeholder="Example: C:\Program Files\Netease\MuMuPlayer"
                        hc:TitleElement.Title="{DynamicResource MuMu12EmulatorPath}"
                        hc:TitleElement.TitlePlacement="Top"
                        Text="{Binding MuMuEmulatorExtras.EmulatorPath}" />
                    <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                        <CheckBox
                            Margin="0,5"
                            Content="{DynamicResource MuMuBridgeConnection}"
                            IsChecked="{Binding MuMuEmulatorExtras.MuMuBridgeConnection}" />
                        <controls:TooltipBlock TooltipText="{DynamicResource MuMuBridgeConnectionTip}" />
                    </StackPanel>
                    <hc:TextBox
                        Margin="0,5"
                        HorizontalContentAlignment="Center"
                        hc:TitleElement.Title="{DynamicResource MuMu12Index}"
                        hc:TitleElement.TitlePlacement="Top"
                        Text="{Binding MuMuEmulatorExtras.Index}"
                        Visibility="{c:Binding MuMuEmulatorExtras.MuMuBridgeConnection}" />
                </StackPanel>
            </StackPanel>

            <!--  截图增强（LDPlayer）  -->
            <StackPanel Visibility="{c:Binding 'ConnectConfig==&quot;LDPlayer&quot; and !UseAttachWindow'}">
                <StackPanel
                    Margin="0,5"
                    HorizontalAlignment="Left"
                    Orientation="Horizontal">
                    <CheckBox Content="{DynamicResource LdExtrasEnabled}" IsChecked="{Binding LdPlayerExtras.Enable}" />
                    <controls:TooltipBlock TooltipText="{DynamicResource LdExtrasEnabledTip}" />
                </StackPanel>
                <StackPanel Visibility="{c:Binding LdPlayerExtras.Enable}">
                    <hc:TextBox
                        Margin="0,5"
                        hc:InfoElement.Placeholder="Example: C:\leidian\LDPlayer9"
                        hc:TitleElement.Title="{DynamicResource LdEmulatorPath}"
                        hc:TitleElement.TitlePlacement="Left"
                        Text="{Binding LdPlayerExtras.EmulatorPath}" />
                    <CheckBox
                        HorizontalAlignment="Left"
                        VerticalContentAlignment="Center"
                        Content="{DynamicResource LdManualSetIndex}"
                        IsChecked="{Binding LdPlayerExtras.ManualSetIndex}" />
                    <hc:TextBox
                        Width="130"
                        Margin="0,5"
                        hc:InfoElement.Placeholder="0"
                        hc:TitleElement.Title="{DynamicResource LdIndex}"
                        hc:TitleElement.TitlePlacement="Left"
                        Text="{Binding LdPlayerExtras.Index}"
                        Visibility="{c:Binding LdPlayerExtras.ManualSetIndex}" />
                </StackPanel>
            </StackPanel>

            <!--  截图测试  -->
            <StackPanel Visibility="{c:Binding '!UseAttachWindow'}">
                <Button
                    Height="30"
                    Margin="0,5"
                    HorizontalAlignment="Left"
                    Command="{s:Action TestLinkAndGetImage}"
                    Content="{DynamicResource ScreenshotTest}" />
                <controls:TextBlock
                    Margin="0,5"
                    HorizontalAlignment="Left"
                    Text="{Binding TestLinkInfo}"
                    TextWrapping="Wrap"
                    Visibility="{c:Binding 'TestLinkInfo!=&quot;&quot;'}" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
